# SCRIPTS Folder

* In this folder are the Python scripts that you need to run in order to generate the RDF knowledge graphs.
* Each script has a number, and needs to be run sequentially from lowest to highest number.